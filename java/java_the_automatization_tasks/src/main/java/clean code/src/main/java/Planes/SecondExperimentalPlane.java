package Planes;

public class SecondExperimentalPlane extends Plane {
    public SecondExperimentalPlane(String model, int maxSpeed, int maxFlightDistance, int maxLoadCapacity) {
        super(model, maxSpeed, maxFlightDistance, maxLoadCapacity);
    }
}